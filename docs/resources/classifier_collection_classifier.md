---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "altr_classifier_collection_classifier Resource - altr"
subcategory: ""
description: |-
  Manages the membership of a single classifier in a classifier collection. Use one instance per classifier (works well with for_each). ALTR-managed collections cannot have classifiers added.
---

# altr_classifier_collection_classifier (Resource)

Manages the membership of a single classifier in a classifier collection. Use one instance per classifier (works well with `for_each`). ALTR-managed collections cannot have classifiers added.

## Example Usage

```terraform
# Copyright (c) ALTR Solutions, Inc.
# SPDX-License-Identifier: Apache-2.0

resource "altr_classifier" "ssn" {
  name              = "SSN"
  description       = "Matches US Social Security Numbers"
  pattern           = "\\d{3}-\\d{2}-\\d{4}"
  minimum_threshold = 70
}

resource "altr_classifier_collection" "pii" {
  name        = "pii_collection"
  description = "PII and sensitive data classifiers"
}

resource "altr_classifier_collection_classifier" "pii_ssn" {
  collection_name = altr_classifier_collection.pii.name
  classifier_name = altr_classifier.ssn.name
}

# Or manage several memberships with for_each over classifiers that
# already exist (e.g. created elsewhere or ALTR-provided).
resource "altr_classifier_collection_classifier" "pii_members" {
  for_each = toset(["Email_Address", "Phone_E164"])

  collection_name = altr_classifier_collection.pii.name
  classifier_name = each.value
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `classifier_name` (String) Name of the classifier to include in the collection. Must not contain ':' (it is the import ID separator).
- `collection_name` (String) Name of the classifier collection. Must not contain ':' (it is the import ID separator).

### Read-Only

- `id` (String) Membership identifier (collection_name:classifier_name).
